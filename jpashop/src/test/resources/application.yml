# test resources의 있는 파일이 우선으로 사용됨
# 내용 없어도 스프링 부트가 메모리로 처리해줌
spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 모드로 사용
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace